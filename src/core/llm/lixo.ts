// return JSON.stringify({
//   role: "system",
//   content: await Deno.readTextFile("./src/core/llm/prompts/scrum.txt"),
// });

// export const discordValue = async () => {
//   const discordToken = getEnv("DISCORD_TOKEN");

//   getDiscordClient().on("messageCreate", (message) => {
//     if (message.channel.id === "1343704126806294558") {
//       console.log(
//         `[${message.author.username}]: ${message.content} in ${message.channel.timestamp}`,
//       );
//     }
//   });
// };

